Review the code in ch23. There are other agents working in different
chapters, so only modify files in this chapter ch23 and commit your work as
you go, making sure to only commit files in this chapter.

Add a snapshot target to the Makefile for this chapter.

It should extract all the code to .c and .h, and the extracted file should go in
a directory named snapshot. Any handwritten code in the snapshot (e.g., the main
test driver) should have a prominent comment in the header indicating that it is
handwritten. All other files should be generated by F* + krml. Include a
Makefile in the snapshot directory that can be used to build and run the tests
in the snapshot.

Commit the snapshot to the repo if it works.
